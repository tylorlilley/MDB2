// Draw Player and Cape
draw_with_ladder_silhouette();
/*
if (visible && has_cape && cape_depth >= depth) { draw_cape_graphics(); }
//if !(draw_quadrants_with_silhouttes(sprite_index, image_index, virtual_x, virtual_y, get_left_value(), main_palette)) { draw_dynamic_object(); }
if (visible && has_cape && cape_depth < depth) { draw_cape_graphics(); }
*/