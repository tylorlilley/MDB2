if (debug_enabled) {
	var _speed = game_get_speed(gamespeed_fps);
	if (_speed > 1) { game_set_speed(window_fps_setting, gamespeed_fps); }
	else { game_set_speed(window_fps_setting, gamespeed_fps); }
}