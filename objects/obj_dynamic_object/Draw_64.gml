 /*
 var _str = (state == PLAYER_STATES.FALL) ? "FA" : "ST"
 if (state == PLAYER_STATES.SWIM) { _str = "FL" }
 if (state == PLAYER_STATES.SURFACE) { _str = "SR" }
 draw_text(x, y, _str + ":" + string(fall_timer));
 */
 